export default function SecurityPanel() {
  return (
    <div className="security-panel">
      <h4>Security and accountability by default</h4>
      <p>
        FairSlot records who changed what and when, so your organization can
        audit operations without extra tooling.
      </p>
      <div className="tag-row">
        <span className="tag">Role-aware access</span>
        <span className="tag">Action-level auditing</span>
        <span className="tag">Request traceability</span>
      </div>
    </div>
  );
}
